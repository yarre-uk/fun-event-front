'use client';

import { Button } from '@/shared/components/ui/button';

function AdminUserPage() {
  return (
    <div>
      <p>Admin page</p>
      <Button>Get weather admin</Button>
      <Button onClick={() => {}}>Get session</Button>
    </div>
  );
}

export default AdminUserPage;
